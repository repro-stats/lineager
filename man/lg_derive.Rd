% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive.R
\name{lg_derive}
\alias{lg_derive}
\title{Derive new variables with documented derivation}
\usage{
lg_derive(data, ..., description)
}
\arguments{
\item{data}{An \code{lg_df} from \code{\link[=lg_tag]{lg_tag()}}.}

\item{...}{Name-value pairs of derivations, passed to \code{\link[dplyr:mutate]{dplyr::mutate()}}.}

\item{description}{Character. \strong{Required.} What is being derived and from
what source. E.g. \code{"AVAL: numeric conversion of LBORRES; LBSTRESN used where LBORRES is missing or non-numeric"}.}
}
\value{
An \code{lg_df} with derived variables added.
}
\description{
Works exactly like \code{\link[dplyr:mutate]{dplyr::mutate()}} but records a derivation description
in the session operation log. Use this when computing ADaM analysis
variables from SDTM source variables.
}
\examples{
lg_start()
lb <- lg_tag(
  data.frame(USUBJID = "01-001", LBORRES = "12.4",
             LBSTRESN = 12.4, stringsAsFactors = FALSE),
  dataset_id = "LB", domain = "LB"
)

lb_derived <- lg_derive(
  lb,
  AVAL = dplyr::coalesce(LBSTRESN, suppressWarnings(as.numeric(LBORRES))),
  description = "AVAL: LBSTRESN; numeric LBORRES where LBSTRESN is missing"
)

}
\seealso{
\code{\link[=lg_filter]{lg_filter()}}, \code{\link[=lg_join]{lg_join()}}, \code{\link[=lg_spec]{lg_spec()}}
}
