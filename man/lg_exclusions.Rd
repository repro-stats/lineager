% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{lg_exclusions}
\alias{lg_exclusions}
\title{Retrieve the exclusion registry}
\usage{
lg_exclusions(population = NULL, dataset_id = NULL, verbose = TRUE)
}
\arguments{
\item{population}{Character or \code{NULL}. Filter to a specific population flag
(e.g. \code{"SAFFL"}). \code{NULL} returns all exclusions.}

\item{dataset_id}{Character or \code{NULL}. Filter to a specific dataset.}

\item{verbose}{Logical. If \code{TRUE} (default), prints a count summary.}
}
\value{
A \code{data.frame} with columns: \code{excl_id}, \code{op_id}, \code{dataset_id},
\code{lid}, \code{usubjid}, \code{reason}, \code{reason_code}, \code{population}, \code{excluded_at}.
}
\description{
Returns all exclusions recorded by \code{\link[=lg_filter]{lg_filter()}} calls during the active
session as a flat \code{data.frame}. This is the data underlying the subject
disposition listing : every excluded subject, with their USUBJID, the
reason they were excluded, and which population the exclusion relates to.
}
\examples{
lg_start()
adsl <- lg_tag(
  data.frame(USUBJID = c("01","02","03"), RANDFL = c("Y","N","Y")),
  dataset_id = "ADSL"
)
lg_filter(adsl, RANDFL == "Y",
          reason = "Not randomised", population = "RANDFL")

lg_exclusions()

}
\seealso{
\code{\link[=lg_trace]{lg_trace()}}, \code{\link[=lg_disposition]{lg_disposition()}}
}
