% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{lg_filter}
\alias{lg_filter}
\title{Filter a tagged dataset with mandatory exclusion documentation}
\usage{
lg_filter(data, ..., reason, population = NULL, reason_code = NULL)
}
\arguments{
\item{data}{An \code{lg_df} from \code{\link[=lg_tag]{lg_tag()}}.}

\item{...}{Filter conditions, passed to \code{\link[dplyr:filter]{dplyr::filter()}}.}

\item{reason}{Character. \strong{Mandatory.} Why these rows are being excluded.
E.g. \code{"Not randomised (RANDFL != 'Y')"}.}

\item{population}{Character or \code{NULL}. Which population flag this exclusion
relates to (e.g. \code{"SAFFL"}). Used to group the exclusion listing.}

\item{reason_code}{Character or \code{NULL}. Short controlled-vocabulary code
for this exclusion (e.g. \code{"NOT_RANDOMISED"}). Useful for programmatic
querying of the exclusion registry.}
}
\value{
An \code{lg_df} containing only the rows that passed the filter.
Excluded rows are recorded in the session store.
}
\description{
Works exactly like \code{\link[dplyr:filter]{dplyr::filter()}} but requires a \code{reason} for every
exclusion. Rows that do not meet the filter conditions are captured in the
session exclusion registry with their USUBJID (if present), lineage ID,
and the documented reason.
}
\details{
\code{reason} has no default. Undocumented exclusions are a compliance failure —
this is enforced at the R level, not by convention.
}
\examples{
lg_start()
adsl <- lg_tag(
  data.frame(USUBJID = c("01", "02", "03"),
             RANDFL  = c("Y", "N", "Y"),
             SAFFL   = c("Y", "N", "Y")),
  dataset_id = "ADSL"
)

adsl_rand <- lg_filter(
  adsl,
  RANDFL == "Y",
  reason      = "Not randomised (RANDFL != 'Y')",
  reason_code = "NOT_RANDOMISED",
  population  = "RANDFL"
)

}
\seealso{
\code{\link[=lg_tag]{lg_tag()}}, \code{\link[=lg_exclusions]{lg_exclusions()}}, \code{\link[=lg_disposition]{lg_disposition()}}
}
