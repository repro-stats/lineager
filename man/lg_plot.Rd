% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineage.R
\name{lg_plot}
\alias{lg_plot}
\title{Render a lineage graph}
\usage{
lg_plot(lineage, output = NULL)
}
\arguments{
\item{lineage}{An \code{lg_lineage} object from \code{\link[=lg_lineage]{lg_lineage()}}.}

\item{output}{Character or \code{NULL}. File path for DOT output (e.g.
\code{"pipeline.dot"}). When \code{NULL} (default), renders inline using
\code{DiagrammeR::grViz()} if available, otherwise prints the DOT source
to the console.}
}
\value{
The \code{lg_lineage} object, invisibly.
}
\description{
Renders the lineage graph returned by \code{\link[=lg_lineage]{lg_lineage()}} as an interactive
inline widget (using \code{DiagrammeR} if installed), or writes the DOT source
to a file for rendering with Graphviz externally.
}
\examples{
\dontrun{
lg_start()
pts <- lg_tag(data.frame(USUBJID = c("P01","P02"),
                          eligible = c(TRUE, FALSE),
                          stringsAsFactors = FALSE),
              dataset_id = "PATIENTS")
lg_filter(pts, eligible, reason = "Not eligible")
lin <- lg_lineage()
lg_plot(lin)
lg_end()
}

}
\seealso{
\code{\link[=lg_lineage]{lg_lineage()}}
}
