% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population.R
\name{lg_population}
\alias{lg_population}
\title{Document and apply a population flag}
\usage{
lg_population(
  data,
  flag_var,
  label,
  definition,
  incl_criteria,
  excl_criteria = NULL
)
}
\arguments{
\item{data}{An \code{lg_df} containing the flag variable.}

\item{flag_var}{Character. The flag variable name (e.g. \code{"SAFFL"}).}

\item{label}{Character. Human label (e.g. \code{"Safety Analysis Flag"}).}

\item{definition}{Character. Plain-English definition for regulatory
reviewers (e.g. \code{"All randomised subjects who received at least one dose of study medication"}).}

\item{incl_criteria}{Character vector of inclusion criteria as R expressions
or plain English. At least one required.}

\item{excl_criteria}{Character vector of explicit exclusion criteria.
\code{NULL} if there are none beyond failing inclusion.}
}
\value{
\code{data}, invisibly (for pipe use).
}
\description{
Population flags (SAFFL, ITTFL, PPROTFL, and custom flags) are first-class
objects in \code{lineager}. Every flag must carry its inclusion criteria,
exclusion criteria, and plain-English definition : the information needed
to reconstruct the Reviewer's Guide population section automatically.
}
\details{
The flag variable must already exist in \code{data}. \code{lg_population()} documents
it; it does not compute it. Compute the flag first with \code{\link[=lg_derive]{lg_derive()}}, then
call \code{lg_population()} to register its definition.
}
\examples{
lg_start()
adsl <- lg_tag(
  data.frame(
    USUBJID = c("01", "02", "03"),
    RANDFL = c("Y", "N", "Y"), EXOCCUR = c("Y", "N", "Y"),
    SAFFL = c("Y", "N", "Y")
  ),
  dataset_id = "ADSL"
)

lg_population(
  adsl,
  flag_var = "SAFFL",
  label = "Safety Analysis Flag",
  definition = "All randomised subjects who received at least one dose",
  incl_criteria = c("RANDFL == 'Y'", "EXOCCUR == 'Y'"),
  excl_criteria = "No study drug administered (EXOCCUR != 'Y')"
)

}
\seealso{
\code{\link[=lg_filter]{lg_filter()}}, \code{\link[=lg_disposition]{lg_disposition()}}, \code{\link[=lg_report]{lg_report()}}
}
