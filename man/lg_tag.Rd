% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tag.R
\name{lg_tag}
\alias{lg_tag}
\title{Tag a dataset to begin lineage tracking}
\usage{
lg_tag(data, dataset_id, domain = NULL, label = NULL, source = NULL)
}
\arguments{
\item{data}{A \code{data.frame} or \code{tibble}.}

\item{dataset_id}{Character. Short identifier for this dataset, e.g.
\code{"LB"}, \code{"ADLB"}, \code{"ADSL"}. Used as the prefix in lineage IDs and
in report output.}

\item{domain}{Character or \code{NULL}. CDISC domain code if applicable
(e.g. \code{"DM"}, \code{"LB"}, \code{"AE"}). Used for SDTM-to-ADaM mapping and
Reviewer's Guide output.}

\item{label}{Character or \code{NULL}. Human-readable label for the dataset
(e.g. \code{"Laboratory test results"}). Used in reports.}

\item{source}{Character or \code{NULL}. Source file or system description.}
}
\value{
An \code{lg_df} object : a \code{data.frame} with a \code{.__lid__} column and
lineage metadata stored in attributes.
}
\description{
Assigns a unique lineage identifier (\code{.__lid__}) to every row and registers
the dataset in the active session store. This is the entry point to
\code{lineager} : all other functions require a tagged data frame.
}
\details{
The \code{.__lid__} column is added at position 1 and is preserved through
\code{\link[=lg_filter]{lg_filter()}}, \code{\link[=lg_derive]{lg_derive()}}, and \code{\link[=lg_join]{lg_join()}} operations. It allows
every row in any downstream dataset to be traced back to its origin.
}
\examples{
lg_start()

dm <- data.frame(
  USUBJID = c("01-001", "01-002", "01-003"),
  AGE     = c(34L, 52L, 47L),
  SEX     = c("M", "F", "M")
)

dm_tagged <- lg_tag(dm,
  dataset_id = "DM", domain = "DM",
  label = "Demographics"
)
dm_tagged

}
\seealso{
\code{\link[=lg_filter]{lg_filter()}}, \code{\link[=lg_derive]{lg_derive()}}, \code{\link[=lg_trace]{lg_trace()}}
}
