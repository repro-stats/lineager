% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{lg_trace}
\alias{lg_trace}
\title{Trace a subject's complete lineage journey}
\usage{
lg_trace(usubjid, verbose = TRUE)
}
\arguments{
\item{usubjid}{Character. The subject identifier to trace. Must match a
value of \code{USUBJID} in at least one tagged dataset.}

\item{verbose}{Logical. If \code{TRUE} (default), prints a formatted trace
to the console.}
}
\value{
A list (invisibly) with components:
\describe{
\item{\code{usubjid}}{The traced subject ID.}
\item{\code{datasets}}{Character vector of dataset IDs the subject appears in.}
\item{\code{operations}}{Data frame of operations applied to datasets
containing this subject.}
\item{\code{exclusions}}{Data frame of exclusion records for this subject,
or a zero-row data frame if none.}
\item{\code{populations}}{Named list of population flag values for this
subject across all registered populations.}
}
}
\description{
Given a USUBJID (or a \code{.__lid__} value), returns the complete history of
that subject across all tagged datasets and operations in the session:
which datasets they appear in, which operations they passed through or were
excluded by, and which population flags apply to them.
}
\details{
This is the key regulatory tracing capability : a reviewer can ask
"show me everything that happened to subject 01-042" and get a complete,
programmatically generated answer.
}
\examples{
lg_start()
adsl <- lg_tag(
  data.frame(
    USUBJID = c("01", "02", "03"),
    RANDFL = c("Y", "N", "Y")
  ),
  dataset_id = "ADSL"
)
lg_filter(adsl, RANDFL == "Y",
  reason = "Not randomised", population = "RANDFL"
)

lg_trace("02")

}
\seealso{
\code{\link[=lg_exclusions]{lg_exclusions()}}, \code{\link[=lg_disposition]{lg_disposition()}}
}
