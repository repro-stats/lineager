% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tag.R
\name{[.lg_df}
\alias{[.lg_df}
\title{Subset an \code{lg_df}, preserving lineage attributes}
\usage{
\method{[}{lg_df}(x, i, j, drop = FALSE)
}
\arguments{
\item{x}{An \code{lg_df} object.}

\item{i}{Row index, as in \verb{[.data.frame}.}

\item{j}{Column index, as in \verb{[.data.frame}.}

\item{drop}{Ignored : \code{lg_df} subsetting always behaves as though
\code{drop = FALSE}. See Details.}
}
\value{
An \code{lg_df} with lineage attributes preserved (or a plain
\code{data.frame}/vector for subsetting operations where preservation
is not applicable, matching normal \verb{[.data.frame} fallback behaviour).
}
\description{
Subset an \code{lg_df}, preserving lineage attributes
}
\details{
\verb{[.lg_df} deliberately forces \code{drop = FALSE}, unlike base \verb{[.data.frame}.
This means single-column subsetting (e.g. \code{df[, "col"]}) returns a
one-column \code{lg_df}/\code{data.frame} rather than a bare vector, so the lineage
attributes are never silently lost through ordinary subsetting. Use
\code{df[[col]]} or \code{lg_id(df)} when a plain vector is what you actually want.
}
