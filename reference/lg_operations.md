# Retrieve the operation log as a data frame

Retrieve the operation log as a data frame

## Usage

``` r
lg_operations(verbose = FALSE)
```

## Arguments

- verbose:

  Logical. Print count summary. Default `FALSE`.

## Value

A `data.frame` of all recorded operations.
